onPressed: () {
/*Navigator.pushNamed(
                              context,
                              _OrderPageState.routeName,
                               arguments: ScreenArguments(
                                  Icon(Icons.favorite, color: Colors.blue),
                                  Text(dishMenu[i])),
                            */

/*_sharedOrder() async {
    SharedPreferences prefs = await SharedPreferences.getInstance();
    prefs.setBool(_dishOrder[index], true);
    //Return bool
    bool boolValue = prefs.getBool(_dishOrder[index]);
    return boolValue;
  }*/